name: Apply Sheikh Scoring Fix

on:
  push:
    branches: [fix/xo-pregame-qr-lobby]
    paths:
      - '.github/workflows/apply-sheikh-fix.yml'

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/xo-pregame-qr-lobby
          fetch-depth: 0
      - name: Apply Sheikh scoring fix
        run: python tools/fix_sheikh_scoring.py
      - name: Commit fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/games/line_games/line_games.dart test/sheikh_beard_scoring_contract_test.dart
          git commit -m "Fix Sheikh Beard line scoring"
          git push origin HEAD:fix/xo-pregame-qr-lobby
